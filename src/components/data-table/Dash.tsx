import React from 'react';

/** Em-dash placeholder for empty table cells (master handoff Part 2 §4). */
export const Dash: React.FC = () => (
  <span className="text-muted-foreground/40 select-none">—</span>
);
